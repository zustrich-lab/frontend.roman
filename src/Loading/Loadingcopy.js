import React from 'react';
import '../Loading/Loading.css';

function LoadingScreenCopy() {
    return (
        <div className={'loading-screen '}>
            <span className="loader"></span>
        </div>
    );
}

export default LoadingScreenCopy;